{
  "name": "vortexus",
  "version": "0.0.2",
  "description": "A simple library to build awesome web apps",
  "scripts": {
    "build": "rm -rf dist/lib && tsc && tsc --build tsconfig.es5.json",
    "run:example": "npm run build && cd examples && npm run build"
  },
  "author": "ajmalmohad",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
